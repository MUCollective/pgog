---
title: "width-densityplot Notebook"
output: html_notebook
---

```{r}
library(tidyverse)
```

Creating the density plot with width aes the hard way

```{r}
mtcars %>%
  ggplot() + 
  geom_density(aes(x = mpg, y = stat(density)))
```


This is not right

```{r}
mtcars %>%
  ggplot(aes(x = cut(mpg, breaks = 10), y = stat(density))) + 
  geom_density()
```

```{r}
mtcars %>%
  .$mpg %>%
  cut(breaks = 10)
```


```{r}

```

